<%= 
  build app_frame(
    title: t("Modifier le modèle d'entretien"),
    notice:
  ) do 
    render(partial: 'form', locals: { method: :put, url: build_model_url(Reviews::ReviewTemplate, params: @review_template) })
  end
%>