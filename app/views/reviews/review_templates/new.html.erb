<%= 
  build app_frame(
    title: t("Créer un modèle d'entretien"),
    notice:
  ) do 
    render(partial: 'form', locals: { method: :post, url: build_model_url(Reviews::ReviewTemplate, index: true) })
  end
%>