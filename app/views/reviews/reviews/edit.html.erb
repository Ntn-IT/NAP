<%= 
  build app_frame(
    title: t("Modifier l'entretien - #{kind} "),
    notice:,
    bind_to: @review,
    actions: [
      reviews.review_context.help_button
    ],
  ) do 
    render(partial: 'form', locals: { method: :put, url: build_model_url(Reviews::Review, params: @review) })
  end
%>