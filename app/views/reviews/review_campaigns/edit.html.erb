<%= 
  build app_frame(
    title: t("Modifier la campagne d'entretien"),
    notice:
  ) do 
    render(partial: 'form', locals: { method: :put, url: build_model_url(Reviews::ReviewCampaign, params: @review_campaign) })
  end
%>

