
<%= 
  build record_form(
    @user, 
    url:, 
    method: method,
    buttons: [reset_button, save_button]
  ) do |form| %>
    <%= 
      build(
        user_context.form_fields(form, :username, :roles)
      )
    %>
  <% end 
%>